You will need python3 to run this

In the Python Folder is a main.py

run
python3 main.py

inside the c++ folder contains the --release build from Rust

Rust defines the functions that make the Generic

C++ calls the functions, and adds an abstraction layer to allow the C++ functions to be called from Python

This is a test in regaurds to the Viper Engine 
